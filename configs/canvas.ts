export interface CanvasSizeAttributes {
  HEIGHT: number;
  WIDTH: number;
}

export const CANVAS_SIZE: CanvasSizeAttributes = {
  HEIGHT: 427,
  // WIDTH: 195, // for testing
  // HEIGHT: window.innerHeight * 0.6,
  WIDTH: window.innerWidth * 0.2, // for testing
};
